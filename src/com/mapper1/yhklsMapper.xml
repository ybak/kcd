<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper1.yhklsMapper">

	    <select id="findyhklsbyc_name" parameterType="yhkls" resultType="yhkls">
        select * from assess_yhkls
        where c_name=#{c_name}
        </select>
        
          <select id="findyhkls" parameterType="yhkls" resultType="yhkls">
        select * from assess_yhkls
      
        </select>
        
 </mapper>