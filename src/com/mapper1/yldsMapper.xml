<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper1.yldsMapper">

		<select id="findyldsbyid" parameterType="ylds" resultType="ylds">
        select * from yl_ds where ACCOUNT_NO=#{ACCOUNT_NO}
        </select>
        
        <select id="findylds" parameterType="ylds" resultType="ylds">
        select * from yl_ds
        </select>

</mapper>